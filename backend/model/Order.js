// const mongoose = require('mongoose');
// const { Schema, model } = mongoose;

// const orderSchema = new Schema({
//     date: { type: Date, default: Date.now }, // Use Date type for date, set default if needed
//     name: { type: String, required: true }, // Make name required if it's a necessary field
//     email: { type: String, required: true },
//     city: String,
//     street: String,
//     order: [String], // Assuming you're storing an array of strings, adjust if needed
//     price: { type: Number, required: true },
// });

// const Order = model('Order', orderSchema);

// module.exports = Order;
